.center-container
  .nag
    .checkbox
    .borrower-photo
      =image_tag "https://graph.facebook.com/#{@nag.lendee_uid}/picture?width=100&height=100"
    .nag-details
      %h2
        ="#{@nag.lendee_name} owes you #{@nag.item}"
      -if @nag.due_date + 1.second > Time.now + 1.week
        .alert-box
          ="Due: #{@nag.due_date.strftime('%b %-d, %Y')}"
      -else
        .alert-box-high-priority
          ="Due in #{((((@nag.due_date.to_time - Time.now)/60)/60)/24).to_i} days"
  .nag-actions
    =form_for @nag, html: {id: "#nag-complete-form"} do |f|
      =link_to "Edit", edit_nag_url(@nag)
      =link_to "Delete", nag_url(@nag), method: "delete", confirm: "Are you sure you want to delete this nag?"
      %a#show-message-preview{href: "#"}Send a reminder
      -if @nag.completed?
        =f.submit "Re-open", :disable_with => 'Re-opening...', class: "button-to-link"
        =f.hidden_field :completed, value: false
      -else
        =f.submit "Mark as complete", :disable_with => 'Completing...', class: "button-to-link"
        =f.hidden_field :completed, value: true
  .reminder-form
    %h3
      Deadbeat friend? Let us help. We'll send them the following private Facebook message from you.
    %form
      %textarea
        Hi [borrowers's first name]!&#10;&#10;I'm using a new app called Nag to help me keep track of my stuff.&#10;&#10;I was hoping I could get that stuff you owe me back. Otherwise, we can no longer be friends.&#10;&#10;There's some more detail at the link below, and maybe even a funny cat video to pass the time.&#10;&#10;http://localhost:3000/nag/xyzxyzxyz123&#10;&#10;Love ya,&#10;[Lenders's first name]
      .reminder-buttons
        %submit.btn
          Submit
        %button.btn#cancel-reminder
          Cancel
